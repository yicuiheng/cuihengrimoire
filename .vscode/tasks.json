{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${file}",
                "-Iinclude",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-std=gnu++17",
                "-Wall",
                "-Wextra",
                "-O2",
                "-fsanitize=undefined"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}